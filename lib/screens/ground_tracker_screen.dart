import 'package:flutter/material.dart';

class GroundTrackerScreen extends StatelessWidget {
  const GroundTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ground Tracker')),
      body: const Center(child: Text('Ground Tracker Coming Soon')),
    );
  }
}
